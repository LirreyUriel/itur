FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY prisma ./prisma
RUN npx prisma generate

COPY assistant ./assistant

ENV NODE_ENV=production
EXPOSE 3003

CMD ["npx", "tsx", "assistant/src/index.ts"]
